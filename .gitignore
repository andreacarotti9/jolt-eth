/upstream/
/.cargo-target*/
/target/
**/target/
*.log
/bench/results/raw/
# Fixtures are regenerated by scripts/fetch-fixtures.sh from a pinned release.
/bench/fixtures/eest/
/bench/fixtures/*.tar.gz
/bench/fixtures/*.tar.zst
/bench/fixtures/*.bin

# Python bytecode from the report scripts.
__pycache__/
*.pyc

# Stale: this crate became a workspace member, so cargo ignores its own lock.
/guests/stateless-validator-jolt/Cargo.lock
