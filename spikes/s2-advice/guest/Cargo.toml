[package]
name = "s2-advice-guest"
version.workspace = true
edition.workspace = true

[features]
guest = []

[dependencies]
jolt = { package = "jolt-sdk", path = "../../../upstream/jolt/jolt-sdk" }

[lib]
test = false
doctest = false
bench = false

[[bin]]
name = "s2-advice-guest"
path = "src/main.rs"
# Only ever linkable for the RISC-V guest target.
required-features = ["guest"]
test = false
bench = false
